package pl.netanel.swt.matrix;

import org.junit.Test;

public class CellSpanTest {
  @Test
  private void basic() {
    ZoneCore zone = new ZoneCore(new SectionCore(Integer.class), new SectionCore(Integer.class));
    zone.setSpan
  }

}
